<div class="modal fade" id="editModal" tabindex="-1" role="dialog" aria-labelledby="editModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="editModalLabel">Edit Service Member</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span>&times;</span>
                </button>
            </div>
            <div class="modal-body" id="modalBodyContent">
                <!-- Dynamic content will be injected here -->
            </div>
            <div class="modal-footer d-flex flex-wrap align-items-center justify-content-end w-100 gap-2">
                <label class="me-1 mb-0">Checked-In by</label>
                <input type="text" id="checkedInBy" name="checkedInBy" class="form-control form-control-sm me-3" style="width: 200px;">

                <label class="me-1 mb-0">Checked-Out by</label>
                <input type="text" id="checkedOutBy" name="checkedOutBy" class="form-control form-control-sm me-3" style="width: 200px;">

                <!-- Print Routing Sheet Button -->
                <button class="btn btn-primary me-2" id="printRoutingSheet" style="display:none">
                    <i class="fa fa-print"></i> Print Routing Sheet
                </button>

                <!-- Checked In -->
                <label for="checkedIn" class="me-1">Checked In</label>
                <select class="form-control me-3" id="checkedIn" name="checkedIn" style="width: auto;">
                    <option value="Yes">Yes</option>
                    <option value="No" selected>No</option>
                </select>

                <!-- Checked Out -->
                <label for="checkedOut" class="me-1">Checked Out</label>
                <select class="form-control me-3" id="checkedOut" name="checkedOut" disabled style="width: auto;">
                    <option value="Yes">Yes</option>
                    <option value="No" selected>No</option>
                </select>

                <!-- Close Button -->
                <button type="button" class="btn btn-secondary text-white me-2" data-dismiss="modal">Close</button>

                <!-- Save Changes Button -->
                <button type="button" id="saveChangesButton" class="btn btn-primary text-white" onclick="saveChangesButton()">Save Changes</button>
            </div>

        </div>
    </div>
</div>

@* <div class="modal fade" id="yesNoModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-custom-width">
        <div class="modal-content">
            <div class="modal-body" id="yesNoModalMessage">
                Are you sure?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" id="yesBtn">Yes</button>
                <button type="button" class="btn btn-secondary" id="noBtn">No</button>
            </div>
        </div>
    </div>
</div> *@

<div class="modal fade" id="yesNoModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-custom-width">
        <div class="modal-content rounded-4 shadow-lg border-0">

            <!-- Custom Colored Header -->
            <div class="modal-header custom-header text-dark rounded-top-4">
                <h5 class="modal-title w-100 text-center" id="yesNoModalTitle">
                    <i class="bi bi-question-circle-fill me-2"></i>
                    Confirmation
                </h5>
            </div>

            <!-- Message Body -->
            <div class="modal-body text-center p-4" id="yesNoModalMessage">
                Are you sure you want to proceed?
            </div>

            <!-- Footer Buttons -->
            <div class="modal-footer justify-content-center border-0 pb-4">
                <button type="button" class="btn custom-yes-btn px-4 rounded-pill" id="yesBtn">Yes</button>
                <button type="button" class="btn btn-outline-secondary px-4 rounded-pill" id="noBtn">No</button>
            </div>
        </div>
    </div>
</div>



<style>
    .modal-custom-width {
        max-width: 400px;
        transition: transform 0.3s ease-in-out;
    }

    .modal-content {
        transition: all 0.3s ease;
    }

    /* Custom header color */
    .custom-header {
        background-color: rgba(80, 90, 201, 1);
        border-bottom: none;
    }

    .custom-yes-btn {
        background-color: rgba(80, 90, 201, 1); /* button background */
        color: white; /* text color */
        border: 1px solid rgba(80, 90, 201, 1); /* border color */
    }

        .custom-yes-btn:hover {
            background-color: rgba(80, 90, 201, 0.8); /* hover effect */
            color: white; /* keep text white */
            border-color: rgba(80, 90, 201, 1);
        }

    /* Optional transition effect */
    .modal.fade .modal-dialog {
        transform: translateY(-20px);
        transition: transform 0.3s ease-out;
    }

    .modal.show .modal-dialog {
        transform: translateY(0);
    }


</style>
